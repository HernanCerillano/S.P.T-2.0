// Environment de producción — usado por `ng build --configuration production`.
//
// IMPORTANTE: cuando tengas la URL de Railway, reemplazá el placeholder de abajo.
// Formato esperado: 'https://<servicio>.up.railway.app/api'  (con /api al final).
export const environment = {
  production: true,
  apiUrl: 'https://REEMPLAZAR-CON-URL-DE-RAILWAY/api',
};
